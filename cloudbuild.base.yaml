steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f=Dockerfile.base'
      - '-t=${_IMAGE}:latest'
      - '.'

images: ['${_IMAGE}:latest']

substitutions:
  _IMAGE: ''

options:
  logging: CLOUD_LOGGING_ONLY
